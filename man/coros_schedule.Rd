% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training.R
\name{coros_schedule}
\alias{coros_schedule}
\title{Fetch the training calendar}
\usage{
coros_schedule(
  auth,
  start_day = format(Sys.Date(), "\%Y\%m\%d"),
  end_day = format(Sys.Date() + 14, "\%Y\%m\%d")
)
}
\arguments{
\item{auth}{A \code{coros_auth} object from \code{\link[=coros_login]{coros_login()}}.}

\item{start_day}{Start of the window in \code{"YYYYMMDD"} format.  Defaults to
today.}

\item{end_day}{End of the window in \code{"YYYYMMDD"} format.  Defaults to
14 days from today.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per scheduled item and columns:
\describe{
\item{plan_id}{Training plan identifier.}
\item{id_in_plan}{Item position within the plan.}
\item{plan_program_id}{Associated workout program identifier.}
\item{happen_day}{Scheduled date (\code{Date}).}
\item{name}{Workout name.}
\item{sport_type}{Numeric sport type code.}
\item{sport_name}{Human-readable sport name.}
\item{estimated_min}{Estimated duration in minutes.}
\item{completed}{Logical; \code{TRUE} if the workout has been completed.}
}
}
\description{
Returns a tibble of planned activities from the COROS training schedule
within the given date window.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
auth <- coros_login()

# Upcoming two weeks
schedule <- coros_schedule(auth)

# Narrower window
schedule <- coros_schedule(
  auth,
  start_day = format(Sys.Date(), "\%Y\%m\%d"),
  end_day   = format(Sys.Date() + 7, "\%Y\%m\%d")
)
\dontshow{\}) # examplesIf}
}
