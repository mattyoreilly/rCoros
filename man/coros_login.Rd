% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{coros_login}
\alias{coros_login}
\title{Authenticate with the COROS Training Hub API}
\usage{
coros_login(
  email = Sys.getenv("COROS_EMAIL"),
  password = Sys.getenv("COROS_PASSWORD"),
  region = c("us", "eu")
)
}
\arguments{
\item{email}{COROS account e-mail.  Defaults to the \code{COROS_EMAIL}
environment variable.}

\item{password}{COROS account password.  Defaults to \code{COROS_PASSWORD}.}

\item{region}{API region: \code{"us"} (default) or \code{"eu"}.}
}
\value{
A named list with fields \code{access_token}, \code{user_id}, \code{base_url},
\code{region}, and \code{timestamp}.  Treat this object as opaque and pass it
directly to other \verb{coros_*} functions.
}
\description{
Logs in with an email/password pair and returns an auth object that must be
passed to every other \verb{coros_*} function.  Credentials are read from
environment variables by default so they are never hard-coded in scripts.
}
\details{
Set credentials once per session with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{Sys.setenv(COROS_EMAIL = "you@example.com", COROS_PASSWORD = "secret")
}\if{html}{\out{</div>}}

or add them to your \verb{~/.Renviron} file for persistence.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
auth <- coros_login()  # reads COROS_EMAIL / COROS_PASSWORD from env

# EU region
auth_eu <- coros_login(region = "eu")
\dontshow{\}) # examplesIf}
}
