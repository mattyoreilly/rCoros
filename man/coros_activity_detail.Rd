% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activities.R
\name{coros_activity_detail}
\alias{coros_activity_detail}
\title{Fetch detailed metrics for a single activity}
\usage{
coros_activity_detail(auth, activity_id, sport_type)
}
\arguments{
\item{auth}{A \code{coros_auth} object from \code{\link[=coros_login]{coros_login()}}.}

\item{activity_id}{Activity identifier (from \code{\link[=coros_activities]{coros_activities()}}
\code{activity_id} column).}

\item{sport_type}{Numeric sport type code (from \code{\link[=coros_activities]{coros_activities()}}
\code{sport_type} column).}
}
\value{
A named list with three tibbles:
\describe{
\item{\code{summary}}{One-row tibble with overall activity metrics.}
\item{\code{laps}}{One row per lap with splits.}
\item{\code{hr_zones}}{Heart-rate zone breakdown (seconds and percent).}
}
}
\description{
Returns a list of three tibbles — a one-row summary, per-lap splits, and
time-in-zone heart rate data — for the given activity.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
auth <- coros_login()
acts <- coros_activities(auth)

# Detail for the most recent activity
detail <- coros_activity_detail(
  auth,
  activity_id = acts$activity_id[[1]],
  sport_type  = acts$sport_type[[1]]
)
detail$summary
detail$laps
detail$hr_zones
\dontshow{\}) # examplesIf}
}
