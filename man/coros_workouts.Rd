% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training.R
\name{coros_workouts}
\alias{coros_workouts}
\title{List structured workout programs}
\usage{
coros_workouts(auth)
}
\arguments{
\item{auth}{A \code{coros_auth} object from \code{\link[=coros_login]{coros_login()}}.}
}
\value{
A named list with two tibbles:
\describe{
\item{\code{workouts}}{One row per workout with columns \code{id}, \code{name},
\code{sport_type}, \code{sport_name}, \code{duration_min}, and \code{n_steps}.}
\item{\code{steps}}{One row per step, linked to \code{workouts} via \code{workout_id},
with columns \code{step_name}, \code{duration_s}, \code{duration_min},
\code{power_low_w}, \code{power_high_w}, and \code{sets}.}
}
}
\description{
Retrieves all workout programs stored in the COROS Training Hub, returning
a list of two tidy tibbles: a summary of each workout and its constituent
steps.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
auth <- coros_login()
result <- coros_workouts(auth)
result$workouts
result$steps
\dontshow{\}) # examplesIf}
}
